To build a **world-class Telco Churn Prediction & Visualization Project** that stands out in a portfolio, you need more than just a CSV file. You need a **unified data architecture** (SQL + Python), **interactive dashboards** (Power BI & Tableau), and a **predictive engine** (the app).

Here is a comprehensive **"Project Enhancement Prompt"** that you can copy and paste into an AI tool (like ChatGPT/Claude) to generate the complete code, workflows, and file structures for you.

---

### The Master Prompt (Copy & Paste this)

> **Role:** Act as a Senior Data Scientist and Business Intelligence Architect.
>
> **Project Goal:** Enhance an existing Telco Customer Churn project. I need a complete, production-ready upgrade that includes data engineering, advanced analytics, predictive modeling, and multi-platform visualization.
>
> **Part 1: The Data (SQL & Cleaning)**
> * Generate a synthetic Telco dataset with 10,000 rows.
> * **Columns must include:** CustomerID, Gender, SeniorCitizen, Partner, Dependents, Tenure, PhoneService, MultipleLines, InternetService (Fiber/DSL), OnlineSecurity, OnlineBackup, DeviceProtection, TechSupport, StreamingTV, StreamingMovies, Contract (Month-to-month/One year/Two year), PaperlessBilling, PaymentMethod, MonthlyCharges, TotalCharges, and Churn (Yes/No).
> * **Data Quality:** Introduce intentional inconsistencies (e.g., `TotalCharges` as a string with empty spaces, negative tenure values) and provide a **Python script** to automatically clean these (convert dtypes, impute median for tenure, strip strings).
> * **SQL Schema:** Write a SQL script (PostgreSQL/MySQL) to create a `churn_db` with a table named `customer_churn`. Include indexes on `CustomerID` and `Tenure`.
> * **Output Files:** After cleaning, export the final dataset as `telco_churn_clean.csv` (for Tableau) and `telco_churn_clean.xlsx` (for Power BI).
>
> **Part 2: The Churn Prediction App (Machine Learning)**
> * Build a **Streamlit** or **Flask** web app.
> * The app must load the clean data, train a **Random Forest** and **XGBoost** classifier.
> * **Features for the App:**
>   1. **Live Prediction:** A form where a user inputs contract details and receives a "Churn Risk" percentage immediately.
>   2. **Feature Importance:** A bar chart showing the top 5 drivers of churn (e.g., Tenure, Contract Type, Monthly Charges).
>   3. **SHAP Explainability:** Add a force plot or summary plot to explain *why* a specific customer is predicted to churn.
>
> **Part 3: The Visualizations (Power BI, Tableau, & Python)**
> For the dashboards, I need the following charts to tell the business story:
> 1. **Executive KPI Cards:** Total Customers, Churn Rate %, Average Tenure, Average Monthly Revenue (ARPU).
> 2. **Churn by Contract Type:** A stacked bar chart showing Month-to-month customers churn significantly more than annual contracts.
> 3. **The "Tenure vs. Monthly Charges" Scatter Plot:** Color-coded by Churn, showing that new customers with high monthly bills churn the fastest.
> 4. **Geographic/Services Heatmap:** Show which combination of services (e.g., Fiber optic + No TechSupport) results in the highest churn.
> 5. **Time-Series Simulation:** Simulate churn over 6 months to show trends.
>
> **Part 4: The Workflow & Repository Structure (Git)**
> Provide the exact terminal commands to initialize a Git repository and structure the folders like this:
> ```
> telco-churn-project/
> ├── data/
> │   ├── raw/ (original synthetic data)
> │   └── processed/ (clean.csv and clean.xlsx)
> ├── notebooks/ (EDA.ipynb)
> ├── src/
> │   ├── data_cleaning.py
> │   ├── model_training.py
> │   └── app.py (Streamlit)
> ├── sql/
> │   └── schema_and_queries.sql
> ├── dashboards/
> │   ├── power_bi/ (Provide the .pbix schema/measures)
> │   └── tableau/ (Provide a .twb schema)
> └── README.md
> ```
>
> **Part 5: Specific Enhancement Ask (The "X-Factor")**
> Go beyond standard churn. **Enhance the app** by adding:
> 1. **Customer Lifetime Value (CLV) Calculation:** Calculate the CLV for each customer based on their monthly charges and tenure, and plot the "Churn Rate vs. CLV" to show that the highest-value customers are actually churning.
> 2. **"What-If" Analysis:** In the app, add a slider that allows a manager to simulate "What if we gave a 10% discount to high-risk customers?" and show how the churn rate would decrease.
> 3. **SQL Query Dashboard:** Provide 3 complex SQL queries (using CTEs and Window Functions) that Power BI/Tableau can connect to directly (e.g., `Rank customers by churn probability within each state`).
>
> **Please generate:**
> * The full Python code for the app.
> * The SQL schema.
> * The DAX measures (for Power BI) to calculate Churn Rate dynamically.
> * The Git workflow instructions (staging, committing, branching) to build this project incrementally.

---

### How to use the output from this prompt:
1.  **Data:** The AI will generate the dataset and the cleaning script for you.
2.  **Visualization:** It will describe the measures you need to create. When you import the `telco_churn_clean.xlsx` into **Power BI**, create the following **DAX measure** (which the AI will help you with):
    ```DAX
    Churn Rate = DIVIDE(CALCULATE(COUNT('Table'[CustomerID]), 'Table'[Churn]="Yes"), COUNT('Table'[CustomerID]))
    ```
3.  **For Tableau:** Use the `telco_churn_clean.csv` and create **Dual-Axis Charts** to overlay Churn Rate over Monthly Charges.
4.  **The Git Workflow:** Follow the step-by-step `git add`, `git commit -m "feat: added data cleaning"`, and `git push` commands that the AI provides to show your development progress.

### The "Enhancement" Strategy for your App:
To make the app "very good," focus on the **"Actionability"** of the visualizations:

- **Don't just show the churn rate; show the *Cost* of churn.** (e.g., "We are losing $50,000 MRR from this segment").
- **Add a Cohort Retention Table** (showing how long customers stay per month) – this is the gold standard for Telco visualization.
- **Connect the app to the SQL database** so that the visualizations update in real-time as new data is added.